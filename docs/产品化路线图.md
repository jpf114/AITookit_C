# 产品化路线图

> 创建日期：2026-05-14
> 最后更新：2026-05-20

## 当前状态

- **v1.0.1 发布就绪**（法律合规、CI E2E、全局 vcpkg 最小依赖）
- Debug/Release 编译通过，15 个测试套件 100% 通过
- 安装目录完整，应用可正常启动
- 核心推理链路（加载模型→选择输入→执行推理→查看结果→导出）完整可用
- 多任务推理（检测/分类/分割）完整闭环
- GPU 加速（CUDA Provider）可选
- 应用图标、README、CHANGELOG 已就绪

---

## 第一阶段：堵住致命漏洞

> 不修这些，产品根本不能给用户用

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 1 | 修复中文路径 | ✅ 已完成 | 使用 QFile+cv::imdecode 替代 cv::imread |
| 2 | 修复 NMS 默认值 | ✅ 已完成 | 默认值改为 0.25/0.45 |
| 3 | 修复线程安全 | ✅ 已完成 | QMutex 保护 + 参数化阈值 |

---

## 第二阶段：补齐核心体验

> 让用户能顺畅地完成"第一次推理"

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 4 | 首次运行引导 | ✅ 已完成 | 首页增加"下载示例模型"按钮 |
| 5 | 自动恢复上次模型 | ✅ 已完成 | 启动时从 QSettings 读取并加载上次模型 |
| 6 | 窗口大小/位置持久化 | ✅ 已完成 | 保存/恢复 geometry |
| 7 | 推理期间关闭确认 | ✅ 已完成 | 重写 closeEvent |
| 8 | 术语友好化 | ✅ 已完成 | SpinBox 加 tooltip，术语加中文解释 |

---

## 第三阶段：功能完善

> 让产品从"能用"变成"好用"

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 9 | 批量/视频结果浏览 | ✅ 已完成 | 结果页增加 QListWidget 结果列表，支持切换查看多个推理结果 |
| 10 | 拖放支持 | ✅ 已完成 | 支持拖放图像/视频/文件夹到应用窗口，自动路由到对应推理流程 |
| 11 | 键盘快捷键 | ✅ 已完成 | Ctrl+O 打开图像、Ctrl+S 导出 JSON、Ctrl+Q 退出 |
| 12 | 图像缩放/平移 | ✅ 已完成 | 滚轮缩放（以鼠标为中心）、拖拽平移、双击重置视图、缩放比例提示 |
| 13 | 格式扩展 | ✅ 已完成 | 图像加 tiff/webp，视频加 webm/flv，导出加 TIFF 格式 |
| 14 | 大视频预警 | ✅ 已完成 | 未限制帧数且视频超过 1000 帧时弹出确认对话框 |

---

## 第四阶段：工程化与交付

> 从"开发者工具"升级为"可交付产品"

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 15 | CPack/NSIS 打包 | ✅ 已完成 | 配置 NSIS 安装程序 + ZIP 便携包 |
| 16 | CI/CD | ✅ 已完成 | GitHub Actions 自动构建+测试+打包 |
| 17 | 崩溃报告 | ✅ 已完成 | 集成 MiniDump 生成，崩溃时自动保存转储文件 |
| 18 | vcpkg manifest 模式 | ✅ 已完成 | 启用 VCPKG_MANIFEST_MODE，依赖版本锁定 |
| 19 | 架构重构 | ✅ 已完成 | 提取 AppController 层，MainWindow 仅负责 UI 展示 |
| 20 | 推理后端抽象 | ✅ 已完成 | 定义 InferenceBackend 接口，YoloDetectionModel 实现接口；InferenceTensor 替代 OnnxTensor 实现运行时无关的张量表示 |

---

## 第五阶段：产品体验优化

> 从"能跑"升级为"开箱即用"

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 21 | 下载脚本参数名修复 | ✅ 已完成 | 修复 -OutputDir → -ModelsDir 参数名不匹配 Bug |
| 22 | 默认模型自动发现 | ✅ 已完成 | 首次启动自动扫描 models/ 目录，找到清单+ONNX 则自动加载 |
| 23 | 下载后自动加载 | ✅ 已完成 | 下载示例模型完成后自动加载，无需用户手动选择清单文件 |
| 24 | 快速体验按钮 | ✅ 已完成 | 模型加载后首页显示"快速体验"按钮，一键选择图像并推理 |
| 25 | 后处理策略注册表 | ✅ 已完成 | PostprocessRegistry 根据 decoder 字段分发后处理，为多模型变体扩展铺路 |
| 26 | 推理预热 | ✅ 已完成 | 首次推理前用零张量做 dry run，消除 ONNX Runtime 首次延迟 |
| 27 | 后端能力查询 | ✅ 已完成 | InferenceBackend 新增 supportsGPU()/backendName()，为 GPU 加速铺路 |

---

## 第六阶段：功能增强

> 产品持续演进

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 28 | YOLOv5 解码器 | ✅ 已完成 | 注册 yolo_v5 解码器到 PostprocessRegistry，复用现有后处理逻辑 |
| 29 | 应用版本号显示 | ✅ 已完成 | 窗口标题和首页标题显示版本号，统一通过 QCoreApplication::applicationVersion() 管理 |
| 30 | 模型页信息增强 | ✅ 已完成 | 展示解码器类型（空值显示"yolo_v8（默认）"）和可展开标签列表 |
| 31 | 检测结果按类别过滤 | ✅ 已完成 | 结果页新增类别下拉框，可按类别筛选检测结果 |
| 32 | 批量导出功能 | ✅ 已完成 | 一键导出批量/视频推理结果为 JSON 数组 |

---

## 第七阶段：产品完善

> 产品持续演进

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 33 | YOLOX 解码器 | ✅ 已完成 | 注册 yolo_x 解码器到 PostprocessRegistry，复用 YOLOv8 后处理逻辑 |
| 34 | 关于对话框 | ✅ 已完成 | 设置页添加"关于"按钮，显示应用名称、版本号和版权信息 |
| 35 | 推理性能监控 | ✅ 已完成 | 状态栏显示推理延迟、FPS 和总耗时，批量/视频推理显示平均延迟 |
| 36 | 推理线程数配置 | ✅ 已完成 | 设置页新增推理线程数控件（1-16），OnnxBackend 使用配置的线程数 |

---

## 第八阶段：多任务与架构扩展

> 从单一检测工具升级为多任务 AI 平台

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 33 | 分类模型支持 | ✅ 已完成 | 新增 ClassificationModel + softmax 后处理，InferenceBackend 新增 classify() 接口 |
| 34 | 分割模型支持 | ✅ 已完成 | 新增 SegmentationModel + 掩码后处理，InferenceBackend 新增 segment() 接口 |
| 35 | ModelService 多任务分发 | ✅ 已完成 | loadModel() 根据 task_type 自动创建检测/分类/分割模型 |
| 36 | 模型管理增强 | ✅ 已完成 | 模型页新增"浏览模型目录"按钮，一键打开 models 目录 |
| 37 | 后端动态加载架构 | ✅ 已完成 | BackendPlugin 接口 + BackendRegistry 注册表 + OnnxRuntimePlugin 实现 |

---

## 第九阶段：多任务 UI 闭环

> 让分类和分割模型从"能跑"到"能用"

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 38 | InferenceSummary 扩展 | ✅ 已完成 | 新增 taskType、classifications、segmentations 字段 |
| 39 | InferenceWorker 多任务路由 | ✅ 已完成 | 根据 taskType 自动选择 detect/classify/segment |
| 40 | 分类结果 UI 展示 | ✅ 已完成 | 结果页 Top-K 列表，摘要文本动态显示"类别数" |
| 41 | 分割结果 UI 展示 | ✅ 已完成 | 图像预览叠加半透明掩码，摘要文本动态显示"实例数" |
| 42 | 导出服务多任务适配 | ✅ 已完成 | JSON 导出和渲染图片支持分类/分割结果 |

---

## 第十阶段：GPU 加速与产品打磨

> 从 CPU 推理升级为 GPU 可选，完善用户体验

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 43 | CUDA Provider 支持 | ✅ 已完成 | OnnxBackend 可选 CUDA GPU 加速，条件编译 #ifdef USE_CUDA，自动回退 CPU |
| 44 | GPU 推理设置 | ✅ 已完成 | 设置页新增 GPU 复选框，持久化到 QSettings，传递到模型构造函数链 |
| 45 | 模型目录一键下载 | ✅ 已完成 | 首页新增"模型目录"按钮，ModelCatalogDialog 展示 4 个内置模型，选择后自动下载并加载 |
| 46 | 代码去重 | ✅ 已完成 | 提取 InferenceService::runImageFromMat()，InferenceWorker 委托调用，净减 60 行重复代码 |
| 47 | 版本号升级 | ✅ 已完成 | 0.1.0 → 0.2.0（CMakeLists.txt + vcpkg.json + app_bootstrap.cpp） |

---

## 第十一阶段：v1.0 发布准备

> 产品正式发布前的最后打磨

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 48 | 应用图标 | ✅ 已完成 | 生成 .ico + .rc + .qrc，嵌入 exe 和窗口图标 |
| 49 | 版本号升级 1.0.0 | ✅ 已完成 | CMakeLists.txt + vcpkg.json + app_bootstrap.cpp 同步升级 |
| 50 | README.md | ✅ 已完成 | 项目介绍、功能说明、构建步骤、使用指南 |
| 51 | CHANGELOG.md | ✅ 已完成 | 记录 0.1.0→0.2.0→1.0.0 变更 |
| 52 | CI 自动发布 | ✅ 已完成 | GitHub Actions 自动发布到 GitHub Release，NSIS 安装包 + ZIP 便携包 |
| 53 | 崩溃提示对话框 | ✅ 已完成 | 崩溃时弹出提示，告知用户 dump 文件位置 |

---

## 第十一阶段补充：vcpkg manifest 模式迁移

> 从全局模式迁移到 manifest 模式，使项目可作为完整产品发布

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 54 | vcpkg.json 添加 builtin-baseline | ✅ 已完成 | 锁定依赖版本，确保可重现构建 |
| 55 | vcpkg.json 添加 ICU 依赖 | ✅ 已完成 | 修复 Qt6Core 启动时 ICU DLL 缺失 |
| 56 | 运行时脚本适配 manifest 路径 | ✅ 已完成 | copy_runtime_deps.cmake / install_runtime_deps.cmake.in 优先使用 build/vcpkg_installed |
| 57 | VC++ 运行时部署修复 | ✅ 已完成 | 路径规范化 + 逐文件检查存在性 |
| 58 | QT_QPA_PLATFORM 环境变量修复 | ✅ 已完成 | main.cpp 强制设置 windows 平台，避免 conda 干扰 |
| 59 | 内置模型扩展 | ✅ 已完成 | YOLOv8n/v8s 检测+分类+分割模型及清单 |
| 60 | CI 配置适配 manifest 模式 | ✅ 已完成 | 移除 VCPKG_ROOT 环境变量设置 |
| 61 | 文档同步更新 | ✅ 已完成 | 模型清单规范、YOLO 检测、ONNX 后端、算法总览、README、CHANGELOG |

---

## 第十二阶段：代码质量与国际化

> 产品发布前的代码质量加固与国际化支持

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 54 | 线程安全修复 | ✅ 已完成 | inferenceRunning_/inferenceCancellationRequested_ 改为 std::atomic<bool>，InferenceWorker 改用 deleteLater() |
| 55 | 注册表初始化安全 | ✅ 已完成 | PostprocessRegistry/BackendRegistry 移除 call_once 延迟初始化，构造函数直接创建 impl_ |
| 56 | 推理异常处理增强 | ✅ 已完成 | InferenceSummary 新增 success/errorMessage 字段，异常不再静默吞没 |
| 57 | 输入验证补全 | ✅ 已完成 | ClassificationModel 空图像检查，分类/分割模型构造函数验证输入尺寸 |
| 58 | 分割掩码初始化 | ✅ 已完成 | cv::Mat 改为 cv::Mat::zeros，消除越界时未定义值 |
| 59 | 类型安全修复 | ✅ 已完成 | model_manifest double→int 溢出检查，YOLO 魔数替换为常量 |
| 60 | 代码去重 | ✅ 已完成 | colorizeMask 统一到 core 层，findModelsDirectory 统一搜索路径，预处理复用 |
| 61 | GPU 提示统一 | ✅ 已完成 | 提取 kGpuFallbackMessage 常量，4 处调用统一 |
| 62 | 多语言国际化 | ✅ 已完成 | 180+ 处硬编码中文替换为 tr()，241 条中英文翻译，设置页语言切换 |

---

## 第十三阶段：未来扩展

> 产品持续演进方向

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 63 | TensorRT 后端插件 | 🟡 骨架已就绪 | BackendPlugin 接口 + TensorRtPlugin 占位注册（isAvailable=false） |
| 64 | OpenVINO 后端插件 | 🟡 骨架已就绪 | BackendPlugin 接口 + OpenVinoPlugin 占位注册（isAvailable=false） |
| 65 | 模型仓库在线浏览 | 🟡 部分完成 | resources/model_catalog.json 可配置目录，内置 catalog 为回退 |
